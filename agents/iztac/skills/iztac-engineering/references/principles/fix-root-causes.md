# fix-root-causes

Reproduce the failure and trace its cause before fixing it.

Check state left across restarts, source ownership and all affected callers. A fallback that hides the symptom without preserving the contract is not a repair.
