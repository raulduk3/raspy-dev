# boundary-discipline

Parse and validate where external data enters.

Keep decisions in small functions and IO at the edges. Avoid repeating the same checks inside every helper; persisted files and native API responses remain external inputs on each read.
