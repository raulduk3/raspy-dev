# migrate-callers-then-delete-legacy-apis

Move internal callers and remove the obsolete API in the same planned change.

Retain only compatibility required by actual external users. Historical transcripts and backups are records, not obsolete APIs, and are not deleted as cleanup.
