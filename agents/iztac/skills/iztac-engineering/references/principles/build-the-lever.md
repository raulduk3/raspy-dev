# build-the-lever

Keep a rerunnable tool for substantial mechanical work or verification.

Build the smallest script that performs the migration or demonstrates the behavior. A script is useful when someone can rerun it to verify the result, not merely because it produces a report.
