# subtract-before-you-add

Retire unused behavior before adding its replacement.

Inventory callers and preserve recovery data. Do not retain a deprecated scheduled brief, duplicate dispatcher or empty compatibility module just because it once existed.
